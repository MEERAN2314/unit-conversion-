# Core unit converter library requirements
# No external dependencies required for the core library

# Optional web interface dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Development dependencies (optional)
pytest>=6.0
pytest-cov>=2.0
black>=22.0
flake8>=4.0